## 🔗 Dual Sniffer: работа с двумя устройствами одновременно

Скрипт `dual_sniffer.py` обеспечивает параллельный опрос двух устройств с синхронизацией потоков данных.

### 📊 Характеристики
| Параметр | Значение |
|----------|----------|
| Частота геопозиционера | 200 Гц (чистый CSV) |
| Частота GPS+компас | 10 Гц (NMEA-0183 + ;X,Y,Z) |
| Выходная частота | 200 Гц (интерполяция магнитных данных) |
| Окно синхронизации | ±25 мс (настраивается) |
| Интерполяция | Линейная, по сырым осям X,Y,Z |

### 🚀 Запуск
```bash
# Базовый запуск
python3 dual_sniffer.py --geo-port /dev/ttyUSB0 --mag-port /dev/ttyACM0 --output data.jsonl

# С учётом магнитного склонения и кастомных смещений датчиков
python3 dual_sniffer.py --declination 12.5 --offsets 0,1.5,3.0,4.5 --output stream.jsonl

# Просмотр первых 10 записей из сохранённого файла
head -n 10 data.jsonl | python3 -m json.tool

# Загрузка в pandas для анализа
# python3 -c "import pandas as pd; df = pd.read_json('data.jsonl', lines=True); print(df.head())"
```

### Вывод CLI
```markdown
{
    "timestamp_sys": 1777437852.689127,  # Системное время Unix (момент получения кадра от GEO)
    "timestamp_iso": "2026-04-29T06:44:12.691168",  # Человекочитаемое время ISO 8601

    "sync": {  # Параметры синхронизации двух независимых потоков данных
        "geo_time": 1777437852.689127,      # Время приёма кадра от геопозиционера (200 Гц)
        "mag_time": 1777437852.689127,      # Время ближайшего кадра от GPS/компаса (10 Гц)
        "delta_ms": 0.0,                    # Рассинхронизация потоков в миллисекундах
        "interpolated": false               # true, если данные магнитометра были интерполированы между кадрами
    },

    "geo": {  # Сырые и преобразованные данные с устройства 1 (CSV геопозиционер)
        "timestamp_sys": 1777437852.689127, # Время приёма именно этого CSV-кадра
        "type": "geo_csv",                  # Тип источника данных
        "sensor_id": "222",                 # Идентификатор датчика
        "utc_time": 44407.9,                # Время UTC из кадра (формат HHMMSS.s)
        "state": "A",                       # Статус GPS-фикса: "A"=активен/валид, "V"=поиск/невалид
        "latitude": 54.74153366666666,      # Широта в десятичных градусах (или null при state="V")
        "n_s_indica": "N",                  # Полушарие по широте (N/S)
        "longitude": 20.500887499999997,    # Долгота в десятичных градусах (или null при state="V")
        "e_w_indica": "E",                  # Полушарие по долготе (E/W)
        "ground_spe": 1.449,                # Скорость над землёй (узлы или м/с, зависит от прошивки)
        "position": 217.13,                 # Доп. параметр позиции/курса из CSV-потока
        "date": 290426.0,                   # Дата в формате ДДММГГ
        "f2": 0.0,                          # Дополнительный канал данных 1 (часто: магнитометр/градиент)
        "f3": 0.0,                          # Дополнительный канал данных 2
        "alarm": false                      # Флаг аппаратной тревоги устройства
    },

    "gps": {  # Итоговые/синтезированные навигационные параметры (приоритет: GEO → MAG fallback)
        "latitude": 54.74153366666666,      # Финальная широта для отрисовки/расчётов
        "longitude": 20.500887499999997,    # Финальная долгота для отрисовки/расчётов
        "azimuth_true": 130.25854060106002, # Истинный азимут направления (градусы, 0°=север) [из компаса]
        "course_true": 217.13,              # Курс движения по GPS/GEO (для сравнения или fallback)
        "valid_geo": true                   # true, если у GEO статус "A" (есть GPS-фикс)
    },

    "mag": {  # Данные магнитометра, приведённые к моменту timestamp_sys
        "x": -700.0,                        # Сырое значение оси X (интерполированное)
        "y": 1330.0,                        # Сырое значение оси Y (интерполированное)
        "z": 4935.0,                        # Сырое значение оси Z (интерполированное)
        "azimuth": 130.25854060106002,      # Рассчитанный азимут: atan2(Y,X) + магнитное склонение
        "field_strength": 5158.791040544286 # Модуль вектора магнитного поля: √(x²+y²+z²)
    },

    "sensors": [  # Массив координат 4 датчиков, смещённых вдоль рейки
        {
            "id": 0,                         # Индекс датчика (0 — базовая точка GPS+компас)
            "offset_m": 0.0,                 # Физическое смещение от базовой точки (метры)
            "latitude": 54.7415337,          # Расчётная широта датчика
            "longitude": 20.5008875          # Расчётная долгота датчика
        },
        { 
            "id": 1, "offset_m": 1.2, 
            "latitude": 54.7415254, "longitude": 20.5008754 
            # ⚠️ Смещение рассчитано по азимуту 130.25° + rail_angle (по умолчанию 90°)
            # → вектор рейки направлен перпендикулярно курсу (≈220.25°)
        },
        { 
            "id": 2, "offset_m": 2.4, 
            "latitude": 54.7415172, "longitude": 20.5008634 
        },
        { 
            "id": 3, "offset_m": 3.6, 
            "latitude": 54.741509, "longitude": 20.5008513 
            # ✅ Расстояние ID0→ID3 = 3.6 м строго вдоль линии рейки
        }
    ],

    "valid": true  # true, если для кадра доступны координаты и азимут (годен для отрисовки/экспорта)
}
```

## [MAP](map.html)

## 🗺️ Визуализация расположения датчиков на рейке

Ниже представлена интерактивная карта с расчётными координатами 4 датчиков (азимут 131°, юго-восток).

<div style="border: 1px solid #ddd; border-radius: 8px; overflow: hidden; margin: 20px 0;">
  <iframe 
    src="https://github.com/Inna949Festchuk/SnifferCOM/tree/main/map.html" 
    width="100%" 
    height="500px" 
    style="border: none;"
    title="Карта датчиков рейки"
    loading="lazy">
  </iframe>
</div>

*Наведите на маркеры для просмотра информации, используйте колёсико мыши для масштабирования.*